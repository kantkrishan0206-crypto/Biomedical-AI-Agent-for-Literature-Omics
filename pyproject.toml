[project]
name = "biomedical-ai-agent-platform"
version = "1.0.0"
description = "Research-grade biomedical AI systems platform"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["backend/tests", "tests"]
